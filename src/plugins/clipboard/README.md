# clipboard

> Complex plugin — clipboard text read/write (Tauri plugin-clipboard-manager / navigator.clipboard) behind the SystemResult contract.

## API

<!-- Populated during build -->

## Configuration

<!-- Populated during build -->
