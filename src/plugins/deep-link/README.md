# deep-link

> Complex plugin — deep-link launch URL + runtime delivery (Tauri plugin-deep-link / web launch URL) behind the SystemResult contract. Plugin name string: `deepLink`.

## API

<!-- Populated during build -->

## Configuration

<!-- Populated during build -->
